import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/contexts/AuthContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Kulhudhufushidive - Premier Diving in Maldives",
  description: "Experience world-class diving in the Maldives with Kulhudhufushidive. Professional diving packages, courses, and underwater adventures in crystal-clear waters.",
  keywords: "diving, Maldives, scuba diving, diving courses, underwater photography, dive packages, PADI, diving lessons",
  authors: [{ name: "Kulhudhufushidive" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
